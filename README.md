# Code components starter repo

A simple mono repo to start your next code components project.

Preview app using Tanstack Router, each exported component gets its own page, placed inside the shadow dom.

Components package for the core Webflow components.

Packages directory for eslint and typescript
